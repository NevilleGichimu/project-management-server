const User = require('./userModel');
const Project= require('./projectModel');
const Team = require('./teamModel');
const Task = require('./taskModel');
const Client = require('./clientModel');

module.exports = {User, Project, Team, Task, Client};
